let array = [1, 2, 3, 4, 5];
let index = 0;

while (index < array.length) { // cek jika value index kurang dari total jumlah element array
    console.log(array[index]);
    index++; // tingkatkan value index (increment) untuk loop selanjutnya
}